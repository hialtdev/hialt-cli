# hialt-cli

Plan-Execute-Verify agent CLI with LangGraph orchestration, adversarial critique, and checkpointed runs.

## Install

```bash
uv sync
```

## Usage

```bash
uv run hialt run --task "your task string"
```
